version: '3.8'

services:
  snip-sync:
    build: .
    container_name: snip-sync
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=/data/snippets.db
      - SERVER_ADDR=0.0.0.0:3000
    restart: unless-stopped
